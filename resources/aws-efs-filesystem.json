{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "AWS::EFS::FileSystem",
  "description": "Cloudformation template resource (AWS::EFS::FileSystem)",

  "type": "object",
  "properties": {
    "Properties": {
      "type": "object",
      "properties": {
        "FileSystemTags": {
          "$ref": "../resource-property-types.json#/definitions/aws-properties-efs-filesystem-filesystemtags"
        }
      },
      "additionalProperties": false
    },

    "Type": {
      "type": "string",
      "enum": [
        "AWS::EFS::FileSystem"
      ]
    },

    "DepensOn": {
      "$ref": "../resource-common.json#/definitions/DependsOn"
    },

    "Metadata": {
      "type": "object"
    },

    "DeletionPolicy": {
      "$ref": "../resource-common.json#/definitions/DeletionPolicy"
    },

    "Condition": {
      "type": "string"
    }
  },
  "required": [
    "Type"
  ],
  "additionalProperties": false
}