{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "AWS::SSM::Document",
  "description": "Cloudformation template resource (AWS::SSM::Document)",

  "type": "object",
  "properties": {
    "Properties": {
      "type": "object",
      "properties": {
        "Content": {
          "type": "object"
        }
      },
      "additionalProperties": false
    },

    "Type": {
      "type": "string",
      "enum": [
        "AWS::SSM::Document"
      ]
    },

    "DepdensOn": {
      "$ref": "../resource-common.json#/definitions/DependsOn"
    },

    "Metadata": {
      "type": "object"
    },

    "DeletionPolicy": {
      "$ref": "../resource-common.json#/definitions/DeletionPolicy"
    },

    "Condition": {
      "type": "string"
    }
  },
  "required": [
    "Type",
    "Properties"
  ],
  "additionalProperties": false
}